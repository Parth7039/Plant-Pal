import 'package:flutter/material.dart';
class gardenTile extends StatelessWidget {
  const gardenTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
